# Include optional dependencies for all platforms during install
optional=true

# Support platform-specific packages
fetch-retries=3
fetch-retry-factor=2

# Ensure all platform binaries are downloaded
target_platform=
target_arch=
target_libc=